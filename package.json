{
  "name": "centre-equestre",
  "version": "1.0.0",
  "description": "Application de gestion d'un centre équestre",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve . -p 3000"
  },
  "engines": {
    "node": ">=16"
  }
}